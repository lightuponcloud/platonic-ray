{erl_opts, [debug_info]}.

%% common_lib is a library application that contains header files
%% This minimal configuration is sufficient for header-only libraries
{profiles, [
    {test, [
        {erl_opts, [debug_info, warnings_as_errors]}
    ]}
]}.
